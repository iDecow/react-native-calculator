import { View, Text, StyleSheet, TextInput, Button } from 'react-native';

export default function Multiplication({mult}){

    return(
        <View>
            <Text>{mult.multiplication}</Text>
        </View>

    )

}

const page = StyleSheet.create({


});